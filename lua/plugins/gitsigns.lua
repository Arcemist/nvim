-- Gitsigns.nvim
-- Automagicaly put simbols on lines that differ from te git state,
-- acording to a configured pattern.

return {
  'lewis6991/gitsigns.nvim',
  opts = {
    signs = {
      add = { text = '+' },
      change = { text = '~' },
      delete = { text = '_' },
      topdelete = { text = '‾' },
      changedelete = { text = '~' },
    },
  },
}
